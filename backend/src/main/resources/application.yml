spring:
  profiles:
    default: h2

telegram:
  bot-token: ${TELEGRAM_BOT_TOKEN:}

jwt:
  secret: ${JWT_SECRET:changeme}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
