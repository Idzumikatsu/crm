apiVersion: v2
name: schedule-app
description: Helm chart for Schedule Tracker
version: 0.1.0
appVersion: "1.0"
type: application

# Add PostgreSQL as optional dependency
dependencies:
- name: postgresql
  version: "12.1.6"
  repository: "https://charts.bitnami.com/bitnami"
  condition: postgresql.enabled

